---
import Footer from '../components/Footer.astro';
import Header from '../components/Header';


interface Props {
  title: string;
  description?: string;
  image?: string;
}

const {
  title,
  description = "Manajemen Kinerja Indonesia",
  image = "../../images/logo_mki.png",
} = Astro.props;

var theme = {}
---

<!doctype html>
<html lang="en" class="text-black">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta property="og:image" content={image} />
    <title>{title}</title>
  </head>
      <body class="min-h-screen font-sans overflow-scroll block mr-0 ml-0">
        <Header client:load />
        <slot />
        <Footer />
      </body>
</html>
<style is:global>
  @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap");
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
    --spacing-vertical-separator-breadcrumbs: 24px;
  }

  section {
    @apply p-12 mx-6 sm:mx-48
  }
  
  p{
    @apply leading-8 tracking-wide text-[13px] sm:text-[14px] md:text-[16px]
  }
  html {
    font-family: system-ui, sans-serif;
    background: #ffffff;
    background-size: 224px;
    width: 100%;
    overflow-x: hidden;
    scroll-behavior: smooth;
  }
  h1{
    font-size: 48px;
  }

  h2{
    font-size: 24px;
    font-weight: 600;
    @apply sm:text-[30px] 
  }

   h3{
    font-size: 22px;
    font-weight: 600;
    @apply sm:text-[26px] 
  }

  h4{
    font-size: 20px;
    font-weight: 600;
    @apply sm:text-[22px] 
  }

  h5{
    font-size: 16px;
    font-weight: normal;
    @apply sm:text-[18px] 
  }

  h6{
    font-size: 32px;
    font-weight: normal;
    @apply sm:text-[40px]
  }

  address{
    @apply leading-8 font-normal not-italic
  }

  a{
    text-decoration: underline;
    @apply text-primary
  }
  subtitle{
    color: #A3A3A3;
  }
  code {
    font-family:
      Poppins,
      monospace;
  }

  .subtitle-alt{
    @apply text-dimGray text-sm
  }
  .li-spacing{
    @apply my-2 sm:my-4 md:my-5 underline sm:no-underline
  }

  .href-alt{
    @apply underline sm:no-underline
  }

  .circle {
  	line-height: 0;		/* remove line-height */ 
  	display: inline-block;	/* circle wraps image */
  	margin: 5px;
    border: 4px solid rgba(200,200,200,0.4);
  	border-radius: 50%;	/* relative value */
  	/*box-shadow: 0px 0px 5px rgba(0,0,0,0.4);*/
  	transition: linear 0.25s;
    height: 128px;
    width: 128px;
  }

  .menu{
    clear:both;
    transition: max-height .2s ease-out;
    @apply sm:clear-none sm:float-right sm:max-h-0 flex-col
  }

  li{
    @apply text-left py-1 text-lg
  }

  
</style>
